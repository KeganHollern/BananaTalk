apiVersion: v1
kind: Service
metadata:
  name: bananatalk-backend
  namespace: default
spec:
  selector:
    app: bananatalk-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
